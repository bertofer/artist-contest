// This class is quite useless, as returns exactly the same
// The reason to have it here is for consistency with the organization,
// would easily allow further refactorings of the architecture

class UserView {
  currentUserView (model) {
    return model
  }
}

module.exports = UserView
